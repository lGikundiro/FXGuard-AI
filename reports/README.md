# FXGuard AI Report Index

## Current multicurrency evidence

These files describe the active USD/RWF, EUR/RWF, and KES/RWF application:

| File | Purpose |
|---|---|
| `panel_feedback_response.md` | Claim-by-claim defense response, revised problem/objective, evidence review, reliability decision, and macro-feature plan |
| `flexible_horizon_model_evaluation.md` | Generated fold-by-fold evaluation for the active 1–100-day horizon-aware models, including horizon-band results |
| `testing_and_backtesting_report.md` | Plain-language technical report, limitations, report-ready conclusion, and presentation material |
| `../backend/models/flexible_horizon_model_metadata.json` | Machine-readable flexible-horizon training, backtesting, selection, and holdout metadata |
| `../tests/test_multicurrency_backtesting.py` | Automated tests for forward-only folds, purge gaps, aggregation, and holdout isolation |

The current results are regenerated by:

```text
python scripts/sync_multicurrency_rates.py
python scripts/train_flexible_horizon_models.py
python -m unittest discover -s tests -p "test_*.py" -v
```

## Preserved legacy material

The following files belong to the original USD/RWF-only research workflow and are retained for reproducibility:

| File | Status |
|---|---|
| `model_evaluation.md` | Legacy USD-only model evaluation generated by `scripts/train_models.py` |
| `dataset_summary.md` and `dataset_summary.json` | Legacy USD-only prepared-dataset summary |
| `source_report_text.txt` | Extracted text from the older Word technical report |
| `../backend/models/model_metadata.json` | Legacy USD-only model metadata |
| `../backend/models/risk_model_7d.pkl` and `risk_model_14d.pkl` | Legacy USD-only model files |
| `multicurrency_model_evaluation.md` | Previous fixed 7/14-day multicurrency evaluation |
| `../backend/models/multicurrency_model_metadata.json` | Previous fixed 7/14-day multicurrency metadata |
| `../notebooks/` | Guided original USD/RWF workflow |

These legacy files are not loaded by the active API. The active API loads `risk_model_<CURRENCY>_flexible.pkl` and reads `flexible_horizon_model_metadata.json`.

## External report and presentation

`../FXGuard_AI_Technical_Report_converted.docx` contains the defense report and is updated alongside the active flexible-horizon evidence. The pre-update version is preserved as `../FXGuard_AI_Technical_Report_before_multicurrency_backtesting.docx`.

No PowerPoint presentation file is stored in this repository; the presentation-ready slide and speaker notes are included in `testing_and_backtesting_report.md`.
